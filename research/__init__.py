"""Standalone calibration experiment.

Measures whether the model's probability estimates beat Kalshi's prices
before any capital is committed. Imports nothing from ``engine/`` so it
runs independently of the state of the trading system.
"""
