# Engine dependencies, pinned.
#
# Versions are exact so a fresh install cannot silently pick up a new
# release under a running bot. Resolved and verified green against the full
# test suite; bump deliberately and re-run the suite.
#
#   pip install -r engine/requirements.txt

aiohttp==3.14.3  # async HTTP client for the Kalshi API
pydantic==2.13.5  # Synapse queue schemas
python-dotenv==1.2.3  # loads engine/.env
colorama==0.4.6  # terminal colour on Windows
cryptography==50.0.1  # RSA-PSS request signing for Kalshi
groq==1.7.0
google-genai==2.23.0  # Gemini client for the Brain
supabase==2.31.0  # optional analytics sink
psutil==7.2.2  # process health metrics
ddgs==9.16.0  # DuckDuckGo search for market context
numpy==2.5.3  # Monte Carlo simulation
ruff==0.16.7  # linter
black==26.5.1  # formatter
rich==15.0.0  # CLI display system
